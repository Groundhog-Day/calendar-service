# calendar-service

## CRUD Opersations
The server provide RESTful services with the endpoint `/api/v1/listings` and following CRUD operations:
 * Create / POST
 * Read / GET
 * Update / PUT
 * Delete / DELETE
